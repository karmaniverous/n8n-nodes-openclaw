{
  "name": "sessions_list",
  "label": "Sessions List",
  "description": "List active sessions with optional filters by kind, recency, and message preview.",
  "parameters": {
    "kinds": {
      "type": "string",
      "required": false,
      "description": "Filter by session kinds (comma-separated). Values: main, group, cron, hook, node, other",
      "placeholder": "e.g. main,group",
      "splitAsArray": true
    },
    "limit": {
      "type": "number",
      "required": false,
      "description": "Maximum number of sessions to return"
    },
    "activeMinutes": {
      "type": "number",
      "required": false,
      "description": "Only include sessions active within this many minutes"
    },
    "messageLimit": {
      "type": "number",
      "required": false,
      "description": "Number of recent messages to include per session"
    }
  }
}
